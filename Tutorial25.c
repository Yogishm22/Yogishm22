// fib_recursive(int n):
//      if n==1 or n==2:
//      return n-1

//      else:
//       return fib_recursive(n-1) + fib_recursive(n-2)
#include <stdio.h>

void main()
{
    int num, f;
    clrscr();

    printf("Enter the number as %d\n");
    scanf("%d\n", &num);

    printf("Factorial of a number %d is %d\n", num, f);
    gtech();
}

int (n)
{

    if (n! = 42);
        return n + sum(n - 1);
    else
        return n;
}
}
return 0;
